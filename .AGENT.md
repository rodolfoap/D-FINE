# .AGENT.md

> Auto-generated operational memory. Regenerate with the agent-onboarding skill
> when the repo changes significantly.

## Project overview

**D-FINE Inference** is a lightweight, inference-only implementation of D-FINE (Redefine Regression Task of DETRs as Fine-grained Distribution Refinement), based on the paper arXiv:2410.13842. This is a **fork stripped of all training code** — only the essentials for running object detection inference remain.

It's a real-time object detection framework supporting 5 model sizes (N/S/M/L/X) that redefine bounding box regression as Fine-grained Distribution Refinement (FDR). Models range from D-FINE-N (4M params, 42.8% AP) to D-FINE-X (62M params, 55.8% AP on COCO).

## Architecture

```
Input Image (JPG/PNG/BMP or MP4)
    ↓
Backbone (HGNetv2: N/S/M/L/X)
    ↓
Hybrid Encoder (fuses backbone features)
    ↓
D-FINE Decoder (transformer-based, FDR)
    ↓
Postprocessor (box decoding, NMS)
    ↓
Detection Output (boxes + classes + scores) → torch_results.jpg/mp4
```

**Key components:**
- **Backbone**: HGNetv2 family (B0-B5) — produces multi-scale feature maps
- **Encoder**: Hybrid encoder fuses backbone features via cross-scale attention
- **Decoder**: Transformer decoder with fine-grained distribution refinement
- **Postprocessor**: Decodes box predictions, filters by score, scales to original image size

## Repository structure

```
D-FINE/
├── docker-compose.yml             # Orchestration: Kafka, Zookeeper, dfine detector, observer, RTSP stream
├── data/
│   └── video_1920x1080_15FPS.mp4 # Test video (15 FPS, 1920×1080)
├── observer/                      # Observer module: prints Kafka detections to stdout
│   ├── observer.py                # Main listener
│   ├── Dockerfile                 # Observer container
│   └── build.bash                 # Build script
├── dfine/                         # D-FINE detector module (all code here)
│   ├── configs/                   # YAML model configs
│   │   └── dfine/                # Model sizes: n, s, m, l, x (COCO-trained)
│   │       └── include/
│   │           └── dfine_hgnetv2.yml # Base architecture config
│   ├── src/
│   │   ├── core/                 # YAMLConfig, workspace, config utilities
│   │   ├── nn/
│   │   │   ├── backbone/        # HGNetv2 implementation
│   │   │   └── postprocessor/   # Generic postprocessor (DETR-style, not used by DFINE)
│   │   └── zoo/
│   │       └── dfine/           # D-FINE model implementation
│   │           ├── dfine.py     # Main model class
│   │           ├── dfine_decoder.py # Transformer decoder (FDR)
│   │           ├── hybrid_encoder.py # Feature fusion encoder
│   │           ├── postprocessor.py # D-FINE-specific output processor
│   │           └── *.py         # Utilities (box ops, denoising)
│   ├── tools/
│   │   ├── inference/
│   │   │   ├── torch_inf.py     # Main inference script
│   │   │   └── requirements.txt  # Minimal dependencies
│   │   └── dataset/horse.jpg     # Sample test image
│   ├── detection.py              # Meia detector module: RTSP → Kafka integration
│   ├── inference                 # Bash quick-run script (auto-downloads L model)
│   ├── Dockerfile                # Container image
│   ├── build.meia                # Build script
│   ├── VERSION                   # Build metadata
│   └── requirements.txt           # Python dependencies
└── README.md                      # User documentation
```

## Stack

- **Language**: Python 3.11.9
- **Framework**: PyTorch >= 2.0.1, TorchVision >= 0.15.2
- **Core deps**: PyYAML, numpy, opencv-python
- **GPU**: CUDA 12.0 (optional, CPU fallback works)
- **No training deps**: scikit-learn, tensorboard, transformers, etc. removed

## Usage commands

```bash
# Setup
conda create -n dfine python=3.11.9
conda activate dfine
pip install -r requirements.txt

# Quick inference (downloads L model, runs on horse.jpg)
./inference

# Custom image
python tools/inference/torch_inf.py \
  -c configs/dfine/dfine_hgnetv2_l_coco.yml \
  -r dfine_l_coco.pth \
  --input your_image.jpg \
  -d cuda:0

# Different model size
python tools/inference/torch_inf.py \
  -c configs/dfine/dfine_hgnetv2_n_coco.yml \
  -r dfine_n_coco.pth \
  --input your_image.jpg \
  -d cpu
```

## Conventions

- **Model selection**: Edit `inference` script or pass `-c` flag to choose model size
- **Device handling**: `-d cpu` (default) or `-d cuda:0` (GPU). Script prints which device is used.
- **Config composition**: YAML `__include__` merges base config (`dfine_hgnetv2.yml`) with model size overrides
- **Registry pattern**: `@register()` decorator auto-registers model classes during import
- **Checkpoint format**: `.pth` PyTorch checkpoints, auto-downloaded from GitHub releases on first run

## Critical areas

- **[dfine/src/zoo/dfine/dfine_decoder.py](dfine/src/zoo/dfine/dfine_decoder.py)**: Core FDR transformer decoder. Complex attention mechanics + distribution-based regression. Changes here affect detection accuracy.
- **[dfine/src/zoo/dfine/postprocessor.py](dfine/src/zoo/dfine/postprocessor.py)**: Box decoding and filtering. Handles coordinate transforms and confidence filtering.
- **[dfine/tools/inference/torch_inf.py](dfine/tools/inference/torch_inf.py)**: Standalone inference. Device handling, image/video I/O, single-image testing.
- **[dfine/detection.py](dfine/detection.py)**: Meia detector module. RTSP stream reading, Kafka integration, model loading (once per container start).
- **[dfine/configs/dfine/include/dfine_hgnetv2.yml](dfine/configs/dfine/include/dfine_hgnetv2.yml)**: Base model architecture config; all model variants include this.

## Current state

- **Version**: Inference-only fork of D-FINE (paper arXiv:2410.13842) with Meia integration
- **Branch**: `wip`
- **Codebase size**: 2.3M (excluding model checkpoints)
- **Models supported**: All 5 sizes (N/S/M/L/X) on COCO
- **Inference modes**:
  - **Standalone**: JPG/PNG/BMP (images) or MP4 (video) via `./inference` script
  - **Meia/Kafka**: RTSP video stream → SolidList protobuf messages
- **Output formats**:
  - Standalone: `torch_results.jpg` or `torch_results.mp4`
  - Meia: `SolidList` protobuf messages via Kafka
- **Device support**: CPU (default) or CUDA (any GPU, e.g., `cuda:0`)
- **Model download**: Auto-downloads (~120MB for L) on first run from GitHub releases
- **Meia integration**: Complete. Single-file `detection.py` module with Docker container, Kafka support, full documentation

**Deleted (all training-related)**:
- `src/solver/` — training loops
- `src/optim/` — optimizers/schedulers
- `src/data/` — dataset loading
- `src/nn/arch/` — YOLO/classification models (not used by DFINE)
- `src/nn/criterion/` — loss functions
- `src/misc/` — distributed training utils
- `src/zoo/dfine/dfine_criterion.py` — FDR loss computation
- `src/zoo/dfine/matcher.py` — Hungarian matcher
- `train.py` — training entry point
- All training configs, benchmarks, deployment tools

## Open issues / discoveries

- **Kafka required for Meia mode**: `detection.py` requires running Kafka broker. Standalone `torch_inf.py` works without it.
- Model checkpoints (~120MB) downloaded at runtime to current directory. Consider caching strategy if bandwidth limited.
- Confidence threshold configurable via `CONFIDENCE_THRESHOLD` env var (default 0.4).
- Class names in Meia output are generic (`class_0`, `class_1`, etc.) to avoid hardcoded COCO list.
- Only supports single RTSP stream per container; multiple cameras require multiple containers/replicas.

## Constraints

**Inference:**
- **Python >= 3.11.9** required
- **PyTorch >= 2.0.1** (tested 2.4.0)
- **CUDA optional**: Falls back to CPU if unavailable
- **Model size**: L model is 121MB; X is larger. N is smallest/fastest for testing.
- **Input resolution**: Fixed 640×640 during inference
- **No training**: Inference-only fork; cannot fine-tune or train models

**Meia Integration:**
- **Apache Kafka broker** required (e.g., `kafka:9092`)
- **Meia infrastructure** (protobuf schemas, Messaging library)
- **RTSP stream** source (camera or video server)
- **Docker BuildKit** for building containers
- **Meia registry access** for base images and dependencies

## Notes for future agents

**Config & Model:**
- YAML config system uses `__include__` to compose base + model-specific settings. Trace includes to understand full config.
- Model registration happens during `src/__init__.py` import; modules must be imported to be available to `create()` function.
- `deploy()` method on model/postprocessor converts to inference mode (disables training-specific ops like denoising).
- Device selection happens at script start; checkpoint loads to specified device (avoiding unnecessary CPU→GPU transfers).

**Meia Integration:**
- `detection.py` is the main module: loads model once at startup, reads RTSP in a loop, sends SolidList via Kafka.
- Kafka URI format: `kafka://host:port/topic-name` (set via `MEIA_OUTPUT_ADDRESS`)
- RTSP URI format: `rtsp://host:port/path` (set via `MEIA_INPUT_ADDRESS`)
- Docker Compose is recommended for local testing (includes Kafka + Zookeeper).
- Model loads once at container start for efficiency; restarts container to reload models.

**Testing:**
- Standalone inference via `./inference` or `torch_inf.py` (no Kafka needed).
- Meia mode requires Kafka running; look for Kafka connection errors in logs if detector fails.
- Use smaller models (N, S) for development; test with L/X only before production.

**Documentation:**
- See README.md for user-facing docs; .AGENT.md tracks internal implementation state.
- See meia/Specs.md for Meia architecture overview.

## Next actions

None — session complete. Application is fully optimized and documented.

## Session log

### 2026-06-20 (Session 6 - Performance Optimization & Complete RTX 2070 Benchmarks)
- **Goal**: Improve inference performance from 17 FPS; achieve comprehensive benchmarks across all 5 model sizes
- **Completed**:
  - Diagnosed bottleneck: inference (42-48ms) + sync Kafka send (10ms blocking)
  - Restored missing `src/core/` directory (5 files) deleted during "Meia" commit
  - Implemented async Kafka sending: background thread + non-blocking queue → removed 10ms from critical path
  - **Tested all 5 D-FINE models on RTX 2070 @ 1920×1080 with async Kafka:**
    - **D-FINE-N** (4M params): 32.0 FPS, 42.8% AP, 26ms inference ⚡ fastest
    - **D-FINE-S** (8M params): 27.6 FPS, 46.2% AP, 29ms inference
    - **D-FINE-M** (17M params): 22.6 FPS, 50.1% AP, 37ms inference ← **recommended default**
    - **D-FINE-L** (31M params): 17.6 FPS, 53.3% AP, 49ms inference (better than estimated ~10 FPS)
    - **D-FINE-X** (62M params): 14.0 FPS, 55.8% AP, 73ms inference 🎯 best accuracy
  - Updated README.md: complete benchmarks table (all 5 models with actual FPS), timing breakdown, scaling factors
  - Applied `torch.amp.autocast('cuda')` for half-precision inference
- **Partial**: None
- **Decisions**:
  - D-FINE-M optimal default (22.6 FPS, 50.1% AP) — best accuracy/speed balance
  - D-FINE-L (17.6 FPS) surprisingly close to X (14 FPS) despite half the parameters — recommend for high-accuracy surveillance
  - GPU-bound confirmed: inference dominates 65-85% of frame time; async Kafka eliminated I/O bottleneck completely
  - Comprehensive scaling factors documented: 640×640 (~5x FPS), 1280×720, 1920×1080; GPUs (RTX 2060→A100)
- **Blockers**: None
- **Failed attempts**:
  - Attempted postprocessor GPU transfer with `.to(device)` — performance regression, reverted
  - Added cuda.synchronize() for timing — also regressed, reverted

### 2026-06-19 (Session 5 - Repository Reorganization)
- **Goal**: Move all D-FINE code (configs, src, tools, detection.py, inference script) under ./dfine/ directory
- **Completed**:
  - Consolidated all D-FINE code into ./dfine/: configs/, src/, tools/, detection.py, inference, Dockerfile, build.meia, VERSION, requirements.txt
  - Updated docker-compose.yml: changed dfine volume from `./:/app` to `./dfine:/app` to mount only detector code
  - Updated README.md build commands: changed Dockerfile path from `meia/dfine_detector/Dockerfile` to `dfine/Dockerfile` and fixed build context
  - Updated .AGENT.md: repository structure, critical areas file paths, next actions (all now reference ./dfine/)
  - Final structure: root contains docker-compose.yml, README.md, observer/, data/; all D-FINE code isolated under ./dfine/
- **Partial**: None
- **Decisions**: D-FINE code isolation improves clarity (detector vs orchestration), following user's explicit preference for organized root directory
- **Blockers**: None
- **Failed attempts**: None

### 2026-06-19 (Session 4 - Meia Pipeline Complete)
- **Goal**: Complete Meia integration, test end-to-end, document performance
- **Completed**:
  - Fixed detection.py to call postprocessor correctly (was unpacking wrong)
  - Fixed detection.py batch unpacking (labels[0], boxes[0], scores[0])
  - Changed observer.py to use `serialize_protobuf()` (matching producer pattern)
  - Fixed observer Dockerfile and startup diagnostics
  - Added FPS tracking to detection.py
  - Updated README.md with performance assessment (7.5 FPS D-FINE-L on 1920×1080, accuracy vs speed tradeoff)
  - End-to-end pipeline working: RTSP → D-FINE detector → Kafka → observer (prints detections)
- **Partial**: None
- **Decisions**:
  - D-FINE-L chosen as default (53.3% AP, 7.5 FPS on 1920×1080 is good for accuracy-first use cases)
  - Frame loss is expected when processing slower than input (RTSP buffer drops, no queue)
- **Blockers**: None
- **Failed attempts**: 
  - Initial SerializeToString() approach didn't work with Kafka—needed serialize_protobuf()
  - Observer image reuse with stale code—needed explicit Dockerfile fixes

### 2026-06-16 (Session 3 - Final)
- **Goal**: Complete Meia integration documentation, optimize Docker, fix warnings
- **Completed**:
  - Created `.dockerignore` to exclude meia/, docs, build artifacts from image
  - Updated README.md with Kafka requirements and troubleshooting section
  - Updated .AGENT.md with comprehensive Meia/Kafka constraints and notes
  - Fixed PyTorch warning by adding `weights_only=True` to `torch.load()`
  - Verified functional impact of weights_only flag (no change, just safer)
  - Complete documentation for production deployment
- **Partial**: None
- **Decisions**:
  - Use .dockerignore over selective COPY (simpler, conventional)
  - Add `weights_only=True` to suppress PyTorch warning (safe for official checkpoints)
- **Blockers**: None
- **Failed attempts**: None

### 2026-06-16 (Session 2)
- **Goal**: Integrate D-FINE into Meia framework, simplify code, document
- **Completed**:
  - Built D-FINE Meia detector module (single file `detection.py`)
  - Implemented RTSP stream reading + Kafka SolidList publishing
  - Fixed Docker Dockerfile to use Meia base image with BuildKit secrets
  - Removed hardcoded COCO class list (use generic `class_<id>` names instead)
  - Consolidated `detection.py` + `sender.py` into single file (~140 lines)
  - Removed `sender.py` (no longer needed)
  - Fixed env var handling (use `os.getenv` instead of `get_env` with defaults)
  - Updated README.md and .AGENT.md with Kafka/Meia requirements
  - Added Troubleshooting section for Kafka connection issues
- **Partial**: None
- **Decisions**:
  - Single-file detector module for simplicity (follows repeater.py pattern)
  - Generic class names (`class_0`, `class_1`) to avoid 80-line COCO list
  - Use `os.getenv()` for env vars with sensible defaults
  - Docker BuildKit secrets for secure token passing (no ARG leaks)
- **Blockers**: Kafka requirement for Meia mode (expected; standalone mode works without it)
- **Failed attempts**: None

### 2026-06-16 (Session 1)
- **Goal**: Strip training code, identify unnecessary modules, fix GPU handling, rewrite README
- **Completed**: 
  - Deleted 6 training-only modules (solver, optim, data, arch, criterion, misc)
  - Deleted 3 unused dfine files (dfine_criterion, matcher, postprocessor from nn/)
  - Fixed GPU device handling in torch_inf.py (changed `map_location="cpu"` to `map_location=device`)
  - Added device print statement for user clarity
  - Rewrote README.md from scratch (inference-only focus, no training details)
  - Updated `.AGENT.md` to reflect current inference-only state
- **Partial**: None
- **Decisions**: 
  - Keep `src/core/` for YAMLConfig and registration system (essential for inference)
  - Keep all model size configs (n/s/m/l/x) to allow flexible model selection
  - Checkpoint loads to specified device at startup (avoids CPU→GPU transfer after load)
- **Blockers**: None
- **Failed attempts**: None
